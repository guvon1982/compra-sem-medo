# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencias e builds
node_modules
dist
dist-ssr
*.local

# Variaveis de ambiente (segredos)
.env
.env.local
.env.*.local
!.env.example

# Cache do Vite e ESLint
.vite/
.eslintcache

# Coverage de testes
coverage/
.nyc_output/

# Pastas e arquivos do Claude Code (sessoes locais — nao versionar)
.claude/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Arquivos de sistema operacional
Thumbs.db
desktop.ini

# Fontes da apresentacao (InDesign + imagens-fonte) — pesadas demais
# para versionar. Mantemos no repo apenas os artefatos finais (capa.png
# e o PDF). Fontes ficam locais; faca backup em outro lugar (Drive/etc).
docs/Apresentacao/Imgs/
docs/Apresentacao/*.indd
docs/Apresentacao/*.idml

# Historico de contexto inicial — anterior ao PRD, conteudo ja absorvido
# por docs/PRD.md e CLAUDE.md. Mantido apenas localmente como referencia
# pessoal do autor (nao agrega ao repositorio).
RESUMO_PARA_CLAUDE_CODE.md
